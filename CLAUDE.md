# 이 저장소에서 일할 때

역량 레지스트리 전용 챗봇이다. 설계 근거와 재설계 이력은
[docs/REBUILD_PLAN.md](docs/REBUILD_PLAN.md)에 있고, 아래 넷은 그 문서에서
뽑아낸 **깨지기 쉬운 전제**다. 코드를 고치기 전에 읽는다.

이전 구현은 이 전제들이 없어서 8,600줄까지 자랐고 결국 버렸다.

## 1. 질의 유형을 정의하지 않는다

자연어를 파서로 계획으로 바꾸지 않는다. 레지스트리 62개 항목의 **모든 필드가
매 턴 프롬프트에 실리고** 모델이 거기서 답한다.

유형을 만들면 유형에 맞지 않는 질문이 생기고, 그 질문은 되묻기로 샌다. 이전
구현이 무너진 자리가 정확히 거기다 — `CLARIFICATION`이라는 배수구가 있었고,
새 표현이 거기 빠질 때마다 규칙을 하나씩 더했다. 표현은 무한하다.

**"유형 하나만 추가하면 되는데"가 떠오르면 그것이 회귀 신호다.**

## 2. 검증은 스키마에서 유도되는 것만 코드로

- 개수, 이름 존재, 부모·자식 관계 → **코드**. 항목이 62개에서 620개가 되어도
  코드량이 늘지 않는다.
- 표현, 어휘, 문장 구조 → **프롬프트**. 코드로 옮기지 않는다.

이전 구현의 `sanitize_topic_summary`가 500줄이 된 것이 두 번째 규칙을 어긴
결과다. 판단이 애매하면 묻는다 — **이 검증은 스키마에서 나오는가, 표현에서
나오는가.**

## 3. LLM은 주장을 옮기고, 참·거짓은 코드가 정한다

`chat/judge.py`의 출력 스키마에 `matches`·`ok` 같은 판정 필드를 두지 않는다.
두는 순간 모델이 자기 답을 통과시킬 수 있다. judge는 "이 숫자가 무엇을
주장하는가"만 말하고 코드가 레지스트리에서 다시 계산한다.

`tests/test_chat_judge.py`가 그 필드의 부재를 직접 검사한다.

## 4. 검증 계층은 참 양성으로 자기 존재를 증명한다

새 검증을 넣거나 기존 검증을 정교화하기 전에 확인한다.

```bash
python evals/spike.py --audit
```

실행 기록에서 **실제로 잡아낸 오류**를 댈 수 없으면 정교화하지 않고 제거한다.
오탐만 쌓는 층을 정교하게 만드는 것이 이전 구현을 무너뜨린 사고방식이다.

현재 숫자 검증은 참 양성 0건, 오탐 11건이다(31회 실행 기준). 갈래를 더하는
대신 걷어내는 방향으로 판단한다.

## 작업 방식

- 축소를 하나 얹을 때마다 `python evals/spike.py --run`으로 53건 전체를 돌린다.
- **판정이 뒤집힌 케이스는 `--repeat 3`으로 다수결을 확인한다.** 같은 프롬프트로도
  결과가 갈리므로 한 번의 통과를 통과로 세지 않는다.
- eval은 프로덕션과 같은 조건이어야 한다. 타임아웃을 덮어쓰거나 대화를 매번 새로
  시작하면 프로덕션에서만 나는 결함을 못 본다 — 실제로 세 건 놓쳤다.
- `pytest`는 외부 API를 호출하지 않는다. 실제 모델 평가는 `evals/spike.py`가
  담당하며 `--run` 없이는 견적만 낸다.
- `evals/results/`는 답변에 레지스트리 정의가 인용되므로 추적하지 않는다.
